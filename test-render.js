import { renderToString } from 'react-dom/server';
import React from 'react';
// Need to mock import.meta.env
// It's a Vite project, we can just use ts-node or something. Let's just grep the code for any potential crashes.
