const fs = require('fs');

const content = fs.readFileSync('src/data/phases.js', 'utf8');

// We will duplicate the object, translate the english one, and create the getPhaseContent function.

// To save time, we will just prompt the model to do it directly since it has the code. Wait.
